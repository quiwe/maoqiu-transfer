name: Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  apk:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Generate Android platform files
        run: flutter create . --platforms=android

      - name: Patch Android APK platform
        run: ./tool/patch_android_platform.sh

      - name: Install Android API 36
        run: yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36"

      - name: Install dependencies
        run: flutter pub get

      - name: Patch Android plugin compile SDK
        run: ./tool/patch_android_pub_cache.sh

      - name: Analyze
        run: flutter analyze

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: maoqiu-transfer-v0.2.1-android-apk
          path: build/app/outputs/flutter-apk/app-release.apk
